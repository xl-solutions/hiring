import { mount } from 'vue-test-utils'
import Counter from '../src/components/pages/stocks/Gains'
import expect from 'expect'

describe('Testing the Gains component', () => {

  it('', () => {
    expect(0).toBe(0)
  })
})