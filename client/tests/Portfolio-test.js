import { mount } from 'vue-test-utils'
import Counter from '../src/components/pages/stocks/Portfolio'
import expect from 'expect'

describe('Testing the Portfolio component', () => {

  it('', () => {
    expect(0).toBe(0)
  })
})