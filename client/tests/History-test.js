import { mount } from 'vue-test-utils'
import Counter from '../src/components/pages/stocks/History'
import expect from 'expect'

describe('Testing the History component', () => {

  it('', () => {
    expect(0).toBe(0)
  })
})import { mount } from 'vue-test-utils'
import Counter from '../src/components/pages/stocks/Hisotory'
import expect from 'expect'

describe('Testing the History component', () => {

  it('', () => {
    expect(0).toBe(0)
  })
})