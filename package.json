{
    "name": "stock-consulter",
    "version": "1.0.0",
    "description": "Combination of front-end with back-end to query stocks",
    "main": "index.js",
    "scripts": {
        "start-api": "cd ./node && npm install && npm start",
        "start-app": "cd ./front && npm install && npm start",
        "test-api": "cd ./node && npm install && npm start",
        "test-app": "cd ./front && npm install && npm start",
        "start": "npm install concurrently && concurrently \"npm run start-api\" \"npm run start-app\""
    },
    "author": "Marco Antonio",
    "dependencies": {
        "concurrently": "^6.2.0"
    }
}
