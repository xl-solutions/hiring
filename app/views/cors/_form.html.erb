<%= form_with(model: cor, local: true) do |form| %>
  <% if cor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cor.errors.count, "error") %> prohibited this cor from being saved:</h2>

      <ul>
      <% cor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :cor_nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
