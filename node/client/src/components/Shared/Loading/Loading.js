import React from 'react';
import Centralize from '../Centralize/Centralize';

const Loading = () => {
  return (
    <Centralize>
      <h4>Carregando...</h4>
    </Centralize>
  );
};

export default Loading;
