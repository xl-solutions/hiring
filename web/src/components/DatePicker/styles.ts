import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;

  #date-picker-label {
    color: var(--white);
  }

  #date-picker {
    color: var(--white);
  }

  svg {
    color: var(--white);
  }
`;
